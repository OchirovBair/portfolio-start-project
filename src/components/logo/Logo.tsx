import React from 'react';
import {Icon} from '../icons/Icon';
import styled from 'styled-components';

export const Logo = () => {
    return (
        <a href={''}>
            <Icon iconId={'codeSvg'}/>
        </a>
    );
};


